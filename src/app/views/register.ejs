<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link rel="stylesheet" href="/resources/css/style.css">
</head>
<body>
    <h1>Registro de usuarios</h1>
    <div class="register-form">
        <form action="register" method="POST">
            <br><br>
            <label for="nom">Nombre Completo</label>
            <input type="text" name="nameComplete" placeholder="Nombre" required>
            <label for="user">Correo electronico</label>
            <input type="text" name="email" placeholder="correo electronico" required>
            <label for="user">Usuario</label>
            <input type="text" name="user" placeholder="usuario o nickname" required>
            <label for="password">Contraseña</label>
            <input type= "password" name="password" id="password" placeholder="Contraseña" required>
            <label for="restricted">Quieres restringir al usuario</label>
            <input type="radio" name="restricted" value="1"/> Si
            <input type="radio" name="restricted" value="0"/> No
            <div style="display:inline;">
              <a href="/" class="btn-cancel"> Cancelar</a><input type="submit" class="btn-save" value="Guardar">
            </div>
        </form>
        
    </div>
    <script src="//cdn.jsdelivr.net/npm/sweetalert2@10"></script>
    <% if (typeof alert != "undefined") {%>
        <script>
            Swal.fire({
                title: '<%= alertTitle %>',
                text: '<%= alertMessage %>',
                icon: '<%= alertIcon %>',
                showConfirmButton: '<%= showConfirmButton %>',
                timer: '<%= timer %>'
            }).then(()=>{
                window.location='/<%= ruta %>'
            })
        </script>
    <%}%>
</body>
</html>